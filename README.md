# Vehicle Maintenance Project (Days 1-4)
This archive contains scaffolding and example code for the 4-day assignment:
- Day 1: FastAPI backend (CRUD) + PostgreSQL + Docker + Alembic + tests
- Day 2: React frontend skeleton + LangChain agent + /chat endpoint
- Day 3: Jenkinsfile and cloud deployment notes (GCP/AWS)
- Day 4: Databricks ETL notebook (Python) + CI/CD hints

## How to use
- Extract the ZIP
- For local development (Day 1): `cd infra && docker compose up --build`
- Backend Swagger UI: http://localhost:8000/docs
- Frontend dev (React): `cd frontend && npm install && npm start`
- See `docs/` for deliverable checklists and Postman collection.

Good luck! - Generated for Vijay
